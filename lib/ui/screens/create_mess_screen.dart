import 'package:flutter/material.dart';

class Create_Mess extends StatelessWidget {
  const Create_Mess({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Center(
          child: Text("Body"),
        ),
      ),
    );
  }
}
